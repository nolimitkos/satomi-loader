pcall(function()
	game:GetService("HttpService"):RequestAsync({Url="https://skulxpdevelopment.vercel.app/api/github?script=SatomiV2&user="..tostring(game:GetService("Players").LocalPlayer.UserId),Method="GET"})
end)

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local SoundService = game:GetService("SoundService")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local ScriptStartTime = tick()

warn("SatomiV2 MM2 Loaded!")
warn("made by koszx.")

task.spawn(function()
	loadstring(game:HttpGet("https://pastefy.app/9gvb9mgt/raw"))()
end)

local function PlayClickSound()
	local Sound = Instance.new("Sound")
	Sound.SoundId = "rbxassetid://6895079853"
	Sound.Volume = 0.5
	Sound.Parent = SoundService
	Sound:Play()
	game:GetService("Debris"):AddItem(Sound, 1)
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SatomiMM2"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 460, 0, 345)
MainFrame.Position = UDim2.new(0.5, -230, 0.5, -172)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
MainFrame.BackgroundTransparency = 0.15
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(255, 105, 180)
MainStroke.Thickness = 1.5
MainStroke.Transparency = 0.3
MainStroke.Parent = MainFrame

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 38)
TopBar.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
TopBar.BackgroundTransparency = 0.9
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TitleContainer = Instance.new("Frame")
TitleContainer.Size = UDim2.new(0, 220, 1, 0)
TitleContainer.Position = UDim2.new(0, 15, 0, 0)
TitleContainer.BackgroundTransparency = 1
TitleContainer.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0.55, 0)
TitleLabel.Position = UDim2.new(0, 0, 0.1, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Satomi V2 [ MM2 ]"
TitleLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
TitleLabel.TextSize = 16
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleContainer

local SubtitleLabel = Instance.new("TextLabel")
SubtitleLabel.Size = UDim2.new(1, 0, 0.35, 0)
SubtitleLabel.Position = UDim2.new(0, 0, 0.6, 0)
SubtitleLabel.BackgroundTransparency = 1
SubtitleLabel.Text = "made by koszx"
SubtitleLabel.TextColor3 = Color3.fromRGB(255, 182, 193)
SubtitleLabel.TextSize = 10
SubtitleLabel.Font = Enum.Font.Gotham
SubtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
SubtitleLabel.Parent = TitleContainer

local DividerLine = Instance.new("Frame")
DividerLine.Size = UDim2.new(1, -80, 0, 1)
DividerLine.Position = UDim2.new(0, 15, 1, -1)
DividerLine.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
DividerLine.BackgroundTransparency = 0.5
DividerLine.BorderSizePixel = 0
DividerLine.Parent = TopBar

local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 50, 0, 30)
CloseButton.Position = UDim2.new(1, -58, 0.5, -15)
CloseButton.BackgroundTransparency = 1
CloseButton.Text = "• • •"
CloseButton.TextColor3 = Color3.fromRGB(255, 105, 180)
CloseButton.TextSize = 18
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Parent = TopBar

local ContentFrame = Instance.new("Frame")
ContentFrame.Name = "Content"
ContentFrame.Size = UDim2.new(1, 0, 1, -38)
ContentFrame.Position = UDim2.new(0, 0, 0, 38)
ContentFrame.BackgroundTransparency = 1
ContentFrame.ClipsDescendants = true
ContentFrame.Parent = MainFrame

local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 95, 1, -12)
Sidebar.Position = UDim2.new(0, 6, 0, 6)
Sidebar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
Sidebar.BackgroundTransparency = 0.7
Sidebar.BorderSizePixel = 0
Sidebar.Parent = ContentFrame

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 12)
SidebarCorner.Parent = Sidebar

local SidebarStroke = Instance.new("UIStroke")
SidebarStroke.Color = Color3.fromRGB(255, 105, 180)
SidebarStroke.Thickness = 1
SidebarStroke.Transparency = 0.5
SidebarStroke.Parent = Sidebar

local TabList = Instance.new("UIListLayout")
TabList.Padding = UDim.new(0, 4)
TabList.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabList.SortOrder = Enum.SortOrder.LayoutOrder
TabList.Parent = Sidebar

local TabPadding = Instance.new("UIPadding")
TabPadding.PaddingTop = UDim.new(0, 8)
TabPadding.PaddingBottom = UDim.new(0, 8)
TabPadding.Parent = Sidebar

local Tabs = {}
local CurrentTab = nil

local function CreateTabButton(name)
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(0.88, 0, 0, 28)
	Button.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	Button.BackgroundTransparency = 0.88
	Button.BorderSizePixel = 0
	Button.Text = ""
	Button.AutoButtonColor = false
	Button.Parent = Sidebar

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 9)
	Corner.Parent = Button

	local BgGradient = Instance.new("UIGradient")
	BgGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 130, 190)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 60, 130))
	})
	BgGradient.Rotation = 135
	BgGradient.Parent = Button

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 105, 180)
	Stroke.Thickness = 1
	Stroke.Transparency = 0.75
	Stroke.Parent = Button

	local Sheen = Instance.new("Frame")
	Sheen.Size = UDim2.new(1, 0, 0.5, 0)
	Sheen.Position = UDim2.new(0, 0, 0, 0)
	Sheen.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	Sheen.BackgroundTransparency = 0.93
	Sheen.BorderSizePixel = 0
	Sheen.ClipsDescendants = false
	Sheen.ZIndex = 2
	Sheen.Parent = Button

	local SheenCorner = Instance.new("UICorner")
	SheenCorner.CornerRadius = UDim.new(0, 9)
	SheenCorner.Parent = Sheen

	local ActiveBar = Instance.new("Frame")
	ActiveBar.Size = UDim2.new(0, 2, 0.55, 0)
	ActiveBar.Position = UDim2.new(0, 4, 0.5, 0)
	ActiveBar.AnchorPoint = Vector2.new(0, 0.5)
	ActiveBar.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	ActiveBar.BackgroundTransparency = 1
	ActiveBar.BorderSizePixel = 0
	ActiveBar.ZIndex = 3
	ActiveBar.Parent = Button

	local ActiveBarCorner = Instance.new("UICorner")
	ActiveBarCorner.CornerRadius = UDim.new(1, 0)
	ActiveBarCorner.Parent = ActiveBar

	local BtnLabel = Instance.new("TextLabel")
	BtnLabel.Size = UDim2.new(1, 0, 1, 0)
	BtnLabel.Position = UDim2.new(0, 0, 0, 0)
	BtnLabel.BackgroundTransparency = 1
	BtnLabel.Text = name
	BtnLabel.TextColor3 = Color3.fromRGB(180, 130, 165)
	BtnLabel.TextSize = 10
	BtnLabel.Font = Enum.Font.GothamSemibold
	BtnLabel.TextXAlignment = Enum.TextXAlignment.Center
	BtnLabel.ZIndex = 3
	BtnLabel.Parent = Button

	Button.MouseEnter:Connect(function()
		if Button.BackgroundTransparency > 0.5 then
			TweenService:Create(Button, TweenInfo.new(0.15), {BackgroundTransparency = 0.7}):Play()
			TweenService:Create(BtnLabel, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(255, 190, 220)}):Play()
			TweenService:Create(Stroke, TweenInfo.new(0.15), {Transparency = 0.4}):Play()
		end
	end)
	Button.MouseLeave:Connect(function()
		if Button.BackgroundTransparency > 0.5 then
			TweenService:Create(Button, TweenInfo.new(0.15), {BackgroundTransparency = 0.88}):Play()
			TweenService:Create(BtnLabel, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(180, 130, 165)}):Play()
			TweenService:Create(Stroke, TweenInfo.new(0.15), {Transparency = 0.75}):Play()
		end
	end)

	return Button, ActiveBar, BtnLabel, Stroke
end

local HomeTabBtn, HomeTabBar, HomeTabLabel, HomeTabStroke = CreateTabButton("Home")
local MainTabBtn, MainTabBar, MainTabLabel, MainTabStroke = CreateTabButton("Main")
local RoleTabBtn, RoleTabBar, RoleTabLabel, RoleTabStroke = CreateTabButton("Role")
local SettingsTabBtn, SettingsTabBar, SettingsTabLabel, SettingsTabStroke = CreateTabButton("Settings")

local TabContent = Instance.new("Frame")
TabContent.Size = UDim2.new(1, -107, 1, -12)
TabContent.Position = UDim2.new(0, 103, 0, 6)
TabContent.BackgroundTransparency = 1
TabContent.ClipsDescendants = true
TabContent.Parent = ContentFrame

local function CreateTab()
	local Tab = Instance.new("ScrollingFrame")
	Tab.Size = UDim2.new(1, 0, 1, 0)
	Tab.BackgroundTransparency = 1
	Tab.Visible = false
	Tab.ScrollBarThickness = 3
	Tab.ScrollBarImageColor3 = Color3.fromRGB(255, 105, 180)
	Tab.ScrollingDirection = Enum.ScrollingDirection.Y
	Tab.AutomaticCanvasSize = Enum.AutomaticSize.Y
	Tab.CanvasSize = UDim2.new(0, 0, 0, 0)
	Tab.Parent = TabContent

	local Layout = Instance.new("UIListLayout")
	Layout.Padding = UDim.new(0, 8)
	Layout.SortOrder = Enum.SortOrder.LayoutOrder
	Layout.Parent = Tab

	local Padding = Instance.new("UIPadding")
	Padding.PaddingLeft = UDim.new(0, 6)
	Padding.PaddingRight = UDim.new(0, 6)
	Padding.PaddingTop = UDim.new(0, 6)
	Padding.PaddingBottom = UDim.new(0, 6)
	Padding.Parent = Tab

	return Tab
end

local HomeTab = CreateTab()
local MainTab = CreateTab()
local RoleTab = CreateTab()
local SettingsTab = CreateTab()

Tabs = {
	{Button = HomeTabBtn, Tab = HomeTab, Bar = HomeTabBar, Label = HomeTabLabel, Stroke = HomeTabStroke},
	{Button = MainTabBtn, Tab = MainTab, Bar = MainTabBar, Label = MainTabLabel, Stroke = MainTabStroke},
	{Button = RoleTabBtn, Tab = RoleTab, Bar = RoleTabBar, Label = RoleTabLabel, Stroke = RoleTabStroke},
	{Button = SettingsTabBtn, Tab = SettingsTab, Bar = SettingsTabBar, Label = SettingsTabLabel, Stroke = SettingsTabStroke}
}

local function SwitchTab(selectedTab)
	for _, tabData in ipairs(Tabs) do
		local btn = tabData.Button
		local bar = tabData.Bar
		local lbl = tabData.Label
		local stroke = tabData.Stroke
		if tabData.Tab == selectedTab then
			tabData.Tab.Visible = true
			TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.25}):Play()
			TweenService:Create(lbl, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
			TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 0}):Play()
			TweenService:Create(bar, TweenInfo.new(0.2), {BackgroundTransparency = 0}):Play()
		else
			tabData.Tab.Visible = false
			TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.88}):Play()
			TweenService:Create(lbl, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(180, 130, 165)}):Play()
			TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 0.75}):Play()
			TweenService:Create(bar, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
		end
	end
	CurrentTab = selectedTab
end

for _, tabData in ipairs(Tabs) do
	tabData.Button.MouseButton1Click:Connect(function()
		PlayClickSound()
		SwitchTab(tabData.Tab)
	end)
end

local function CreateSection(parent, title, size)
	local Section = Instance.new("Frame")
	Section.Name = title.."Section"
	Section.Size = size
	Section.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
	Section.BackgroundTransparency = 0.6
	Section.BorderSizePixel = 0
	Section.Parent = parent

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 12)
	Corner.Parent = Section

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 105, 180)
	Stroke.Thickness = 1
	Stroke.Transparency = 0.4
	Stroke.Parent = Section

	local TitleLabel = Instance.new("TextLabel")
	TitleLabel.Size = UDim2.new(1, -16, 0, 22)
	TitleLabel.Position = UDim2.new(0, 10, 0, 8)
	TitleLabel.BackgroundTransparency = 1
	TitleLabel.Text = title
	TitleLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
	TitleLabel.TextSize = 12
	TitleLabel.Font = Enum.Font.GothamBold
	TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
	TitleLabel.Parent = Section

	local ContentFrame = Instance.new("Frame")
	ContentFrame.Name = "Content"
	ContentFrame.Size = UDim2.new(1, -12, 1, -34)
	ContentFrame.Position = UDim2.new(0, 6, 0, 30)
	ContentFrame.BackgroundTransparency = 1
	ContentFrame.Parent = Section

	local ListLayout = Instance.new("UIListLayout")
	ListLayout.Padding = UDim.new(0, 6)
	ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
	ListLayout.Parent = ContentFrame

	return Section, ContentFrame
end

local function CreateShimmerButton(parent, text)
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(1, 0, 0, 34)
	Button.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	Button.BackgroundTransparency = 0.6
	Button.BorderSizePixel = 0
	Button.Text = ""
	Button.AutoButtonColor = false
	Button.Parent = parent

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 10)
	Corner.Parent = Button

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 182, 193)
	Stroke.Thickness = 1
	Stroke.Transparency = 0.2
	Stroke.Parent = Button

	local Gradient = Instance.new("UIGradient")
	Gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 140, 200)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 80, 160))
	})
	Gradient.Rotation = 135
	Gradient.Parent = Button

	local GlassSheen = Instance.new("Frame")
	GlassSheen.Size = UDim2.new(1, 0, 0.5, 0)
	GlassSheen.Position = UDim2.new(0, 0, 0, 0)
	GlassSheen.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	GlassSheen.BackgroundTransparency = 0.88
	GlassSheen.BorderSizePixel = 0
	GlassSheen.ZIndex = 2
	GlassSheen.Parent = Button

	local SheenCorner = Instance.new("UICorner")
	SheenCorner.CornerRadius = UDim.new(0, 10)
	SheenCorner.Parent = GlassSheen

	local BtnLabel = Instance.new("TextLabel")
	BtnLabel.Size = UDim2.new(1, 0, 1, 0)
	BtnLabel.BackgroundTransparency = 1
	BtnLabel.Text = text
	BtnLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	BtnLabel.TextSize = 11
	BtnLabel.Font = Enum.Font.GothamBold
	BtnLabel.ZIndex = 3
	BtnLabel.Parent = Button

	local Shimmer = Instance.new("Frame")
	Shimmer.Size = UDim2.new(0, 80, 1, 0)
	Shimmer.Position = UDim2.new(0, -90, 0, 0)
	Shimmer.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	Shimmer.BackgroundTransparency = 0.75
	Shimmer.BorderSizePixel = 0
	Shimmer.ClipsDescendants = false
	Shimmer.ZIndex = 4
	Shimmer.Parent = Button

	local ShimmerCorner = Instance.new("UICorner")
	ShimmerCorner.CornerRadius = UDim.new(0, 10)
	ShimmerCorner.Parent = Shimmer

	local ShimmerGrad = Instance.new("UIGradient")
	ShimmerGrad.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1),
		NumberSequenceKeypoint.new(0.4, 0.5),
		NumberSequenceKeypoint.new(0.6, 0.5),
		NumberSequenceKeypoint.new(1, 1)
	})
	ShimmerGrad.Rotation = 15
	ShimmerGrad.Parent = Shimmer

	Button.MouseEnter:Connect(function()
		TweenService:Create(Button, TweenInfo.new(0.18, Enum.EasingStyle.Quad), {BackgroundTransparency = 0.35}):Play()
		TweenService:Create(Stroke, TweenInfo.new(0.18), {Transparency = 0}):Play()
	end)

	Button.MouseLeave:Connect(function()
		TweenService:Create(Button, TweenInfo.new(0.18, Enum.EasingStyle.Quad), {BackgroundTransparency = 0.6}):Play()
		TweenService:Create(Stroke, TweenInfo.new(0.18), {Transparency = 0.2}):Play()
	end)

	Button.MouseButton1Down:Connect(function()
		TweenService:Create(Button, TweenInfo.new(0.1), {BackgroundTransparency = 0.2}):Play()
		TweenService:Create(BtnLabel, TweenInfo.new(0.1), {TextTransparency = 0.2}):Play()
	end)

	Button.MouseButton1Up:Connect(function()
		PlayClickSound()
		TweenService:Create(Button, TweenInfo.new(0.2), {BackgroundTransparency = 0.45}):Play()
		TweenService:Create(BtnLabel, TweenInfo.new(0.1), {TextTransparency = 0}):Play()
		Shimmer.Position = UDim2.new(0, -90, 0, 0)
		TweenService:Create(Shimmer, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(1, 10, 0, 0)}):Play()
	end)

	return Button
end

local function CreateModernToggle(parent, text, onToggle)
	local Container = Instance.new("Frame")
	Container.Size = UDim2.new(1, 0, 0, 32)
	Container.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
	Container.BackgroundTransparency = 0.5
	Container.BorderSizePixel = 0
	Container.Parent = parent

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Container

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 182, 193)
	Stroke.Thickness = 1
	Stroke.Transparency = 0.5
	Stroke.Parent = Container

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.65, 0, 1, 0)
	Label.Position = UDim2.new(0, 10, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = Color3.fromRGB(200, 200, 210)
	Label.TextSize = 11
	Label.Font = Enum.Font.GothamSemibold
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Container

	local ToggleBg = Instance.new("Frame")
	ToggleBg.Size = UDim2.new(0, 44, 0, 22)
	ToggleBg.Position = UDim2.new(1, -54, 0.5, -11)
	ToggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
	ToggleBg.BackgroundTransparency = 0.3
	ToggleBg.BorderSizePixel = 0
	ToggleBg.Parent = Container

	local ToggleCorner = Instance.new("UICorner")
	ToggleCorner.CornerRadius = UDim.new(1, 0)
	ToggleCorner.Parent = ToggleBg

	local ToggleKnob = Instance.new("Frame")
	ToggleKnob.Size = UDim2.new(0, 16, 0, 16)
	ToggleKnob.Position = UDim2.new(0, 3, 0.5, -8)
	ToggleKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	ToggleKnob.BorderSizePixel = 0
	ToggleKnob.Parent = ToggleBg

	local KnobCorner = Instance.new("UICorner")
	KnobCorner.CornerRadius = UDim.new(1, 0)
	KnobCorner.Parent = ToggleKnob

	local Enabled = false

	local ClickArea = Instance.new("TextButton")
	ClickArea.Size = UDim2.new(1, 0, 1, 0)
	ClickArea.BackgroundTransparency = 1
	ClickArea.Text = ""
	ClickArea.Parent = Container

	ClickArea.MouseButton1Click:Connect(function()
		PlayClickSound()
		Enabled = not Enabled
		if Enabled then
			TweenService:Create(ToggleBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 105, 180)}):Play()
			TweenService:Create(ToggleKnob, TweenInfo.new(0.2), {Position = UDim2.new(0, 25, 0.5, -8)}):Play()
			Label.TextColor3 = Color3.fromRGB(255, 105, 180)
		else
			TweenService:Create(ToggleBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(60, 60, 70)}):Play()
			TweenService:Create(ToggleKnob, TweenInfo.new(0.2), {Position = UDim2.new(0, 3, 0.5, -8)}):Play()
			Label.TextColor3 = Color3.fromRGB(200, 200, 210)
		end
		if onToggle then onToggle(Enabled) end
	end)

	return Container, function() return Enabled end, function(state)
		Enabled = state
		if Enabled then
			ToggleBg.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
			ToggleKnob.Position = UDim2.new(0, 25, 0.5, -8)
			Label.TextColor3 = Color3.fromRGB(255, 105, 180)
		else
			ToggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
			ToggleKnob.Position = UDim2.new(0, 3, 0.5, -8)
			Label.TextColor3 = Color3.fromRGB(200, 200, 210)
		end
	end
end

local function CreateToggleWithSlider(parent, text, defaultValue, onToggle)
	local MainContainer = Instance.new("Frame")
	MainContainer.Size = UDim2.new(1, 0, 0, 70)
	MainContainer.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
	MainContainer.BackgroundTransparency = 0.5
	MainContainer.BorderSizePixel = 0
	MainContainer.Parent = parent

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 10)
	Corner.Parent = MainContainer

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 182, 193)
	Stroke.Thickness = 1
	Stroke.Transparency = 0.5
	Stroke.Parent = MainContainer

	local TopRow = Instance.new("Frame")
	TopRow.Size = UDim2.new(1, 0, 0, 32)
	TopRow.BackgroundTransparency = 1
	TopRow.Parent = MainContainer

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.6, 0, 1, 0)
	Label.Position = UDim2.new(0, 10, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = Color3.fromRGB(200, 200, 210)
	Label.TextSize = 11
	Label.Font = Enum.Font.GothamSemibold
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = TopRow

	local ToggleBg = Instance.new("Frame")
	ToggleBg.Size = UDim2.new(0, 44, 0, 22)
	ToggleBg.Position = UDim2.new(1, -54, 0.5, -11)
	ToggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
	ToggleBg.BackgroundTransparency = 0.3
	ToggleBg.BorderSizePixel = 0
	ToggleBg.Parent = TopRow

	local ToggleCorner = Instance.new("UICorner")
	ToggleCorner.CornerRadius = UDim.new(1, 0)
	ToggleCorner.Parent = ToggleBg

	local ToggleKnob = Instance.new("Frame")
	ToggleKnob.Size = UDim2.new(0, 16, 0, 16)
	ToggleKnob.Position = UDim2.new(0, 3, 0.5, -8)
	ToggleKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	ToggleKnob.BorderSizePixel = 0
	ToggleKnob.Parent = ToggleBg

	local KnobCorner = Instance.new("UICorner")
	KnobCorner.CornerRadius = UDim.new(1, 0)
	KnobCorner.Parent = ToggleKnob

	local SliderRow = Instance.new("Frame")
	SliderRow.Size = UDim2.new(1, 0, 0, 32)
	SliderRow.Position = UDim2.new(0, 0, 0, 34)
	SliderRow.BackgroundTransparency = 1
	SliderRow.Parent = MainContainer

	local SliderBar = Instance.new("Frame")
	SliderBar.Size = UDim2.new(1, -70, 0, 4)
	SliderBar.Position = UDim2.new(0, 10, 0.5, -2)
	SliderBar.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
	SliderBar.BackgroundTransparency = 0.5
	SliderBar.BorderSizePixel = 0
	SliderBar.Parent = SliderRow

	local SliderBarCorner = Instance.new("UICorner")
	SliderBarCorner.CornerRadius = UDim.new(1, 0)
	SliderBarCorner.Parent = SliderBar

	local SliderFill = Instance.new("Frame")
	SliderFill.Size = UDim2.new(0.5, 0, 1, 0)
	SliderFill.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	SliderFill.BorderSizePixel = 0
	SliderFill.Parent = SliderBar

	local SliderFillCorner = Instance.new("UICorner")
	SliderFillCorner.CornerRadius = UDim.new(1, 0)
	SliderFillCorner.Parent = SliderFill

	local InputBox = Instance.new("TextBox")
	InputBox.Size = UDim2.new(0, 45, 0, 22)
	InputBox.Position = UDim2.new(1, -55, 0.5, -11)
	InputBox.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
	InputBox.BackgroundTransparency = 0.3
	InputBox.BorderSizePixel = 0
	InputBox.Text = "50"
	InputBox.TextColor3 = Color3.fromRGB(255, 105, 180)
	InputBox.TextSize = 11
	InputBox.Font = Enum.Font.GothamSemibold
	InputBox.Parent = SliderRow

	local InputCorner = Instance.new("UICorner")
	InputCorner.CornerRadius = UDim.new(0, 6)
	InputCorner.Parent = InputBox

	local Enabled = false
	local Value = defaultValue or 50

	local function UpdateSlider(val)
		Value = math.clamp(tonumber(val) or (defaultValue or 50), 0, 200)
		SliderFill.Size = UDim2.new(Value / 200, 0, 1, 0)
		InputBox.Text = tostring(Value)
	end

	InputBox.Text = tostring(Value)
	SliderFill.Size = UDim2.new(Value / 200, 0, 1, 0)

	local ClickArea = Instance.new("TextButton")
	ClickArea.Size = UDim2.new(1, 0, 0, 32)
	ClickArea.Position = UDim2.new(0, 0, 0, 0)
	ClickArea.BackgroundTransparency = 1
	ClickArea.Text = ""
	ClickArea.ZIndex = 5
	ClickArea.Parent = MainContainer

	ClickArea.MouseButton1Click:Connect(function()
		PlayClickSound()
		Enabled = not Enabled
		if Enabled then
			TweenService:Create(ToggleBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 105, 180)}):Play()
			TweenService:Create(ToggleKnob, TweenInfo.new(0.2), {Position = UDim2.new(0, 25, 0.5, -8)}):Play()
			Label.TextColor3 = Color3.fromRGB(255, 105, 180)
		else
			TweenService:Create(ToggleBg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(60, 60, 70)}):Play()
			TweenService:Create(ToggleKnob, TweenInfo.new(0.2), {Position = UDim2.new(0, 3, 0.5, -8)}):Play()
			Label.TextColor3 = Color3.fromRGB(200, 200, 210)
		end
		if onToggle then onToggle(Enabled) end
	end)

	InputBox.FocusLost:Connect(function()
		UpdateSlider(InputBox.Text)
	end)

	local dragging = false
	SliderBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			local pos = input.Position.X
			local barPos = SliderBar.AbsolutePosition.X
			local barSize = SliderBar.AbsoluteSize.X
			local percent = math.clamp((pos - barPos) / barSize, 0, 1)
			UpdateSlider(math.floor(percent * 200))
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end
	end)

	local function SetToggleState(state, val)
		Enabled = state
		if Enabled then
			ToggleBg.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
			ToggleKnob.Position = UDim2.new(0, 25, 0.5, -8)
			Label.TextColor3 = Color3.fromRGB(255, 105, 180)
		else
			ToggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
			ToggleKnob.Position = UDim2.new(0, 3, 0.5, -8)
			Label.TextColor3 = Color3.fromRGB(200, 200, 210)
		end
		if val ~= nil then
			UpdateSlider(val)
		end
		if onToggle then onToggle(Enabled) end
	end

	return MainContainer, function() return Enabled end, function() return Value end, SetToggleState, UpdateSlider
end

local HomeSection, HomeContent = CreateSection(HomeTab, "Game Info & Profile", UDim2.new(1, 0, 0, 175))
local FeaturesSection, FeaturesContent = CreateSection(HomeTab, "Anchor", UDim2.new(1, 0, 0, 68))

local FPSLabel = Instance.new("TextLabel")
FPSLabel.Size = UDim2.new(0.5, -5, 0, 20)
FPSLabel.BackgroundTransparency = 1
FPSLabel.Text = "FPS: --"
FPSLabel.TextColor3 = Color3.fromRGB(180, 180, 190)
FPSLabel.TextSize = 12
FPSLabel.Font = Enum.Font.Gotham
FPSLabel.TextXAlignment = Enum.TextXAlignment.Left
FPSLabel.Parent = HomeContent

local PlayersLabel = Instance.new("TextLabel")
PlayersLabel.Size = UDim2.new(0.5, -5, 0, 20)
PlayersLabel.BackgroundTransparency = 1
PlayersLabel.Text = "Players: 0"
PlayersLabel.TextColor3 = Color3.fromRGB(180, 180, 190)
PlayersLabel.TextSize = 12
PlayersLabel.Font = Enum.Font.Gotham
PlayersLabel.TextXAlignment = Enum.TextXAlignment.Left
PlayersLabel.Parent = HomeContent

local TimeLabel = Instance.new("TextLabel")
TimeLabel.Size = UDim2.new(1, 0, 0, 20)
TimeLabel.BackgroundTransparency = 1
TimeLabel.Text = "Session: 00:00:00"
TimeLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
TimeLabel.TextSize = 12
TimeLabel.Font = Enum.Font.GothamSemibold
TimeLabel.TextXAlignment = Enum.TextXAlignment.Left
TimeLabel.Parent = HomeContent

spawn(function()
	while wait(0.5) do
		if FPSLabel and FPSLabel.Parent then
			FPSLabel.Text = "FPS: "..math.floor(workspace:GetRealPhysicsFPS())
			PlayersLabel.Text = "Players: "..#Players:GetPlayers()
			local elapsed = tick() - ScriptStartTime
			local hours = math.floor(elapsed / 3600)
			local minutes = math.floor((elapsed % 3600) / 60)
			local seconds = math.floor(elapsed % 60)
			TimeLabel.Text = string.format("Session: %02d:%02d:%02d", hours, minutes, seconds)
		end
	end
end)

local AvatarRow = Instance.new("Frame")
AvatarRow.Size = UDim2.new(1, 0, 0, 55)
AvatarRow.BackgroundTransparency = 1
AvatarRow.Parent = HomeContent

local AvatarImage = Instance.new("ImageLabel")
AvatarImage.Size = UDim2.new(0, 50, 0, 50)
AvatarImage.Position = UDim2.new(0, 0, 0.5, -25)
AvatarImage.BackgroundColor3 = Color3.fromRGB(255, 240, 245)
AvatarImage.BackgroundTransparency = 0.5
AvatarImage.Image = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
AvatarImage.Parent = AvatarRow

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = AvatarImage

local UsernameLabel = Instance.new("TextLabel")
UsernameLabel.Size = UDim2.new(1, -58, 0, 22)
UsernameLabel.Position = UDim2.new(0, 58, 0, 6)
UsernameLabel.BackgroundTransparency = 1
UsernameLabel.Text = LocalPlayer.Name
UsernameLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
UsernameLabel.TextSize = 13
UsernameLabel.Font = Enum.Font.GothamBold
UsernameLabel.TextXAlignment = Enum.TextXAlignment.Left
UsernameLabel.Parent = AvatarRow

local RoleLabel = Instance.new("TextLabel")
RoleLabel.Size = UDim2.new(1, -58, 0, 16)
RoleLabel.Position = UDim2.new(0, 58, 0, 30)
RoleLabel.BackgroundTransparency = 1
RoleLabel.Text = "Userscript: Premium"
RoleLabel.TextColor3 = Color3.fromRGB(150, 150, 160)
RoleLabel.TextSize = 10
RoleLabel.Font = Enum.Font.Gotham
RoleLabel.TextXAlignment = Enum.TextXAlignment.Left
RoleLabel.Parent = AvatarRow

local function AddSpacer(parent, height)
	height = height or 8
	local Spacer = Instance.new("Frame")
	Spacer.Size = UDim2.new(1, 0, 0, height)
	Spacer.BackgroundTransparency = 1
	Spacer.BorderSizePixel = 0
	Spacer.Parent = parent
	return Spacer
end

local FloatingButtons = {}
local AnchoredPositions = {}

local AnchorToggle, GetAnchor, SetAnchor = CreateModernToggle(FeaturesContent, "Lock Position Buttons", function(enabled)
	if enabled then
		for _, fb in ipairs(FloatingButtons) do
			AnchoredPositions[fb.Gui.Name] = fb.Frame.Position
			fb.Frame.Position = AnchoredPositions[fb.Gui.Name]
			fb.Anchored = true
			fb.DragHandle.Visible = false
		end
	else
		for _, fb in ipairs(FloatingButtons) do
			fb.Anchored = false
			fb.DragHandle.Visible = true
		end
	end
end)

AddSpacer(FeaturesContent)

local MainTabTwoCol = Instance.new("Frame")
MainTabTwoCol.Size = UDim2.new(1, 0, 0, 290)
MainTabTwoCol.BackgroundTransparency = 1
MainTabTwoCol.BorderSizePixel = 0
MainTabTwoCol.Parent = MainTab

local LocalScriptSection = Instance.new("Frame")
LocalScriptSection.Name = "LocalScriptSection"
LocalScriptSection.Size = UDim2.new(0.5, -4, 1, 0)
LocalScriptSection.Position = UDim2.new(0, 0, 0, 0)
LocalScriptSection.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
LocalScriptSection.BackgroundTransparency = 0.5
LocalScriptSection.BorderSizePixel = 0
LocalScriptSection.ClipsDescendants = true
LocalScriptSection.Parent = MainTabTwoCol

local LSCorner = Instance.new("UICorner")
LSCorner.CornerRadius = UDim.new(0, 12)
LSCorner.Parent = LocalScriptSection

local LSStroke = Instance.new("UIStroke")
LSStroke.Color = Color3.fromRGB(255, 105, 180)
LSStroke.Thickness = 1
LSStroke.Transparency = 0.35
LSStroke.Parent = LocalScriptSection

local LSTitleLabel = Instance.new("TextLabel")
LSTitleLabel.Size = UDim2.new(1, -12, 0, 22)
LSTitleLabel.Position = UDim2.new(0, 10, 0, 8)
LSTitleLabel.BackgroundTransparency = 1
LSTitleLabel.Text = "LocalScript"
LSTitleLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
LSTitleLabel.TextSize = 12
LSTitleLabel.Font = Enum.Font.GothamBold
LSTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
LSTitleLabel.Parent = LocalScriptSection

local LSScrollFrame = Instance.new("ScrollingFrame")
LSScrollFrame.Size = UDim2.new(1, -8, 1, -36)
LSScrollFrame.Position = UDim2.new(0, 4, 0, 34)
LSScrollFrame.BackgroundTransparency = 1
LSScrollFrame.ScrollBarThickness = 2
LSScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 105, 180)
LSScrollFrame.ScrollingDirection = Enum.ScrollingDirection.Y
LSScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
LSScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
LSScrollFrame.BorderSizePixel = 0
LSScrollFrame.Parent = LocalScriptSection

local LSListLayout = Instance.new("UIListLayout")
LSListLayout.Padding = UDim.new(0, 5)
LSListLayout.SortOrder = Enum.SortOrder.LayoutOrder
LSListLayout.Parent = LSScrollFrame

local LSPad = Instance.new("UIPadding")
LSPad.PaddingTop = UDim.new(0, 4)
LSPad.PaddingBottom = UDim.new(0, 4)
LSPad.PaddingLeft = UDim.new(0, 2)
LSPad.PaddingRight = UDim.new(0, 2)
LSPad.Parent = LSScrollFrame

local LocalScriptContent = LSScrollFrame

local MainESPSection = Instance.new("Frame")
MainESPSection.Name = "MainESPSection"
MainESPSection.Size = UDim2.new(0.5, -4, 1, 0)
MainESPSection.Position = UDim2.new(0.5, 4, 0, 0)
MainESPSection.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
MainESPSection.BackgroundTransparency = 0.5
MainESPSection.BorderSizePixel = 0
MainESPSection.ClipsDescendants = true
MainESPSection.Parent = MainTabTwoCol

local ESPCorner = Instance.new("UICorner")
ESPCorner.CornerRadius = UDim.new(0, 12)
ESPCorner.Parent = MainESPSection

local ESPStroke = Instance.new("UIStroke")
ESPStroke.Color = Color3.fromRGB(255, 105, 180)
ESPStroke.Thickness = 1
ESPStroke.Transparency = 0.35
ESPStroke.Parent = MainESPSection

local ESPTitleLabel = Instance.new("TextLabel")
ESPTitleLabel.Size = UDim2.new(1, -12, 0, 22)
ESPTitleLabel.Position = UDim2.new(0, 10, 0, 8)
ESPTitleLabel.BackgroundTransparency = 1
ESPTitleLabel.Text = "MAIN ESP"
ESPTitleLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
ESPTitleLabel.TextSize = 12
ESPTitleLabel.Font = Enum.Font.GothamBold
ESPTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
ESPTitleLabel.Parent = MainESPSection

local ESPScrollFrame = Instance.new("ScrollingFrame")
ESPScrollFrame.Size = UDim2.new(1, -8, 1, -36)
ESPScrollFrame.Position = UDim2.new(0, 4, 0, 34)
ESPScrollFrame.BackgroundTransparency = 1
ESPScrollFrame.ScrollBarThickness = 2
ESPScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 105, 180)
ESPScrollFrame.ScrollingDirection = Enum.ScrollingDirection.Y
ESPScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ESPScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ESPScrollFrame.BorderSizePixel = 0
ESPScrollFrame.Parent = MainESPSection

local ESPListLayout = Instance.new("UIListLayout")
ESPListLayout.Padding = UDim.new(0, 5)
ESPListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ESPListLayout.Parent = ESPScrollFrame

local ESPPad = Instance.new("UIPadding")
ESPPad.PaddingTop = UDim.new(0, 4)
ESPPad.PaddingBottom = UDim.new(0, 4)
ESPPad.PaddingLeft = UDim.new(0, 2)
ESPPad.PaddingRight = UDim.new(0, 2)
ESPPad.Parent = ESPScrollFrame

local MainESPContent = ESPScrollFrame

local AntiFlingEnabled = false
local AntiFlingBtn = CreateShimmerButton(LocalScriptContent, "Anti Fling")

AntiFlingBtn.MouseButton1Click:Connect(function()
	AntiFlingEnabled = true
	loadstring(game:HttpGet("https://raw.githubusercontent.com/sytcal/antifling/main/9999"))()
end)

local WalkConnection
local WalkspeedControl, GetWalkspeedEnabled, GetWalkspeedValue, SetWalkspeed, SetWalkspeedValue
WalkspeedControl, GetWalkspeedEnabled, GetWalkspeedValue, SetWalkspeed, SetWalkspeedValue = CreateToggleWithSlider(LocalScriptContent, "Walkspeed", 17, function(enabled)
	if enabled then
		if WalkConnection then WalkConnection:Disconnect() end
		WalkConnection = RunService.RenderStepped:Connect(function()
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
				LocalPlayer.Character.Humanoid.WalkSpeed = GetWalkspeedValue()
			end
		end)
	else
		if WalkConnection then WalkConnection:Disconnect() WalkConnection = nil end
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
			LocalPlayer.Character.Humanoid.WalkSpeed = 16
		end
	end
end)

local JumpConnection
local JumpBoostControl, GetJumpBoostEnabled, GetJumpBoostValue, SetJumpBoost, SetJumpBoostValue
JumpBoostControl, GetJumpBoostEnabled, GetJumpBoostValue, SetJumpBoost, SetJumpBoostValue = CreateToggleWithSlider(LocalScriptContent, "JumpBoost", 50, function(enabled)
	if enabled then
		if JumpConnection then JumpConnection:Disconnect() end
		JumpConnection = RunService.RenderStepped:Connect(function()
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
				LocalPlayer.Character.Humanoid.JumpPower = GetJumpBoostValue()
			end
		end)
	else
		if JumpConnection then JumpConnection:Disconnect() JumpConnection = nil end
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
			LocalPlayer.Character.Humanoid.JumpPower = 50
		end
	end
end)

local LP = Players.LocalPlayer

local GunESP, GetGunESP, SetGunESP = CreateModernToggle(MainESPContent, "Gun Highlights")
local MurdererESP, GetMurdererESP, SetMurdererESP = CreateModernToggle(MainESPContent, "Murderer")
local SheriffESP, GetSheriffESP, SetSheriffESP = CreateModernToggle(MainESPContent, "Sheriff")
local InnocentESP, GetInnocentESP, SetInnocentESP = CreateModernToggle(MainESPContent, "Innocent")
local HeroESP, GetHeroESP, SetHeroESP = CreateModernToggle(MainESPContent, "Hero")

SetGunESP(true)
SetMurdererESP(true)
SetSheriffESP(true)
SetInnocentESP(true)
SetHeroESP(true)

local roles = {}
local Murder, Sheriff, Hero = nil, nil, nil
local lastUpdate = 0
local UPDATE_INTERVAL = 0.1

local function IsAlive(playerData)
	return playerData and not playerData.Killed and not playerData.Dead
end

local function GetRoleColor(playerName, playerData)
	if not playerData or playerData.Role == "" or playerData.Role == nil then
		return "Default"
	end
	if playerName == Murder and IsAlive(playerData) then
		if GetMurdererESP() then return Color3.fromRGB(225, 0, 0) end
	elseif playerName == Sheriff and IsAlive(playerData) then
		if GetSheriffESP() then return Color3.fromRGB(0, 0, 225) end
	elseif (playerName == Hero or playerData.Role == "Hero" or playerData.HasGun) and IsAlive(playerData) then
		if GetHeroESP() then return Color3.fromRGB(255, 250, 0) end
	elseif IsAlive(playerData) then
		if GetInnocentESP() then return Color3.fromRGB(0, 225, 0) end
	end
	return nil
end

local function UpdateHighlights()
	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LP then
			local character = player.Character
			if character then
				local highlight = character:FindFirstChild("ExodusHighlight")
				if not highlight then
					highlight = Instance.new("Highlight")
					highlight.Name = "ExodusHighlight"
					highlight.Parent = character
				end
				local playerData = roles[player.Name]
				local res = GetRoleColor(player.Name, playerData)
				if res == "Default" then
					highlight.Enabled = true
					highlight.FillTransparency = 1
					highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
					highlight.OutlineTransparency = 0
				elseif typeof(res) == "Color3" then
					highlight.Enabled = true
					highlight.FillColor = res
					highlight.FillTransparency = 0.5
					highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
					highlight.OutlineTransparency = 0
				else
					highlight.Enabled = false
				end
			end
		end
	end
end

local function UpdateRoles()
	local success, result = pcall(function()
		return ReplicatedStorage:FindFirstChild("GetPlayerData", true):InvokeServer()
	end)
	if success and result then
		roles = result
		Murder, Sheriff, Hero = nil, nil, nil
		for name, data in pairs(roles) do
			if data.Role == "Murderer" then
				Murder = name
			elseif data.Role == "Sheriff" then
				Sheriff = name
			elseif data.Role == "Hero" or data.HasGun then
				Hero = name
			end
		end
	else
		roles = {}
	end
end

task.spawn(function()
	while true do
		local drop = workspace:FindFirstChild("GunDrop", true)
		for _, obj in ipairs(workspace:GetChildren()) do
			if obj:IsA("Model") or obj:IsA("BasePart") then
				local h = obj:FindFirstChild("ExodusGunHighlight")
				if h and obj ~= drop then
					h:Destroy()
				end
			end
		end
		if drop then
			local highlight = drop:FindFirstChild("ExodusGunHighlight")
			if not highlight then
				highlight = Instance.new("Highlight")
				highlight.Name = "ExodusGunHighlight"
				highlight.Parent = drop
				highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
			end
			highlight.FillColor = Color3.fromRGB(255, 255, 0)
			highlight.Enabled = GetGunESP()
		end
		task.wait(0.5)
	end
end)

RunService.RenderStepped:Connect(function(deltaTime)
	lastUpdate = lastUpdate + deltaTime
	if lastUpdate >= UPDATE_INTERVAL then
		lastUpdate = 0
		UpdateRoles()
		UpdateHighlights()
	end
end)

local InnocentSection, InnocentContent = CreateSection(RoleTab, "Innocent", UDim2.new(1, 0, 0, 120))

local MurdererSection, MurdererContent = CreateSection(RoleTab, "Murderer", UDim2.new(1, 0, 0, 310))

local SheriffSection, SheriffContent = CreateSection(RoleTab, "Sheriff/Hero", UDim2.new(1, 0, 0, 100))

local TeleportLobbyBtn = CreateShimmerButton(InnocentContent, "Teleport to Lobby")

TeleportLobbyBtn.MouseButton1Click:Connect(function()
	local localCharacter = game.Players.LocalPlayer.Character
	if localCharacter and localCharacter:FindFirstChild("HumanoidRootPart") then
		localCharacter.HumanoidRootPart.CFrame = CFrame.new(13.808, 507.141, 35.676)
	end
end)

local AutoGetGunToggle, GetAutoGetGun, SetAutoGetGun = CreateModernToggle(InnocentContent, "Automatically Get Gun")

task.spawn(function()
	while true do
		if GetAutoGetGun() then
			local drop = workspace:FindFirstChild("GunDrop", true)
			local character = LP.Character
			local hrp = character and character:FindFirstChild("HumanoidRootPart")
			if drop and hrp then
				firetouchinterest(hrp, drop, 0)
				firetouchinterest(hrp, drop, 1)
			end
		end
		task.wait(0.2)
	end
end)

local selectedTargets = {}

local function CreateTargetDropdown(parent)

	local DropContainer = Instance.new("Frame")
	DropContainer.Size = UDim2.new(1, 0, 0, 40)
	DropContainer.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
	DropContainer.BackgroundTransparency = 0.3
	DropContainer.BorderSizePixel = 0
	DropContainer.ClipsDescendants = false
	DropContainer.ZIndex = 20
	DropContainer.Parent = parent

	local DropContainerCorner = Instance.new("UICorner")
	DropContainerCorner.CornerRadius = UDim.new(0, 12)
	DropContainerCorner.Parent = DropContainer

	local DropContainerStroke = Instance.new("UIStroke")
	DropContainerStroke.Color = Color3.fromRGB(255, 105, 180)
	DropContainerStroke.Thickness = 1
	DropContainerStroke.Transparency = 0.4
	DropContainerStroke.Parent = DropContainer



	local DropButton = Instance.new("TextButton")
	DropButton.Size = UDim2.new(1, 0, 1, 0)
	DropButton.BackgroundTransparency = 1
	DropButton.BorderSizePixel = 0
	DropButton.Text = ""
	DropButton.AutoButtonColor = false
	DropButton.ZIndex = 24
	DropButton.Parent = DropContainer

	local DropIconImg = Instance.new("ImageLabel")
	DropIconImg.Size = UDim2.new(0, 22, 0, 22)
	DropIconImg.Position = UDim2.new(0, 9, 0.5, -11)
	DropIconImg.BackgroundTransparency = 1
	DropIconImg.Image = "rbxassetid://101257615952289"
	DropIconImg.ImageColor3 = Color3.fromRGB(255, 182, 193)
	DropIconImg.ImageTransparency = 0
	DropIconImg.ZIndex = 23
	DropIconImg.Parent = DropContainer

	local DropLabel = Instance.new("TextLabel")
	DropLabel.Size = UDim2.new(1, -68, 1, 0)
	DropLabel.Position = UDim2.new(0, 38, 0, 0)
	DropLabel.BackgroundTransparency = 1
	DropLabel.Text = "Select Target"
	DropLabel.TextColor3 = Color3.fromRGB(180, 130, 165)
	DropLabel.TextSize = 11
	DropLabel.Font = Enum.Font.GothamSemibold
	DropLabel.TextXAlignment = Enum.TextXAlignment.Left
	DropLabel.TextTruncate = Enum.TextTruncate.AtEnd
	DropLabel.ZIndex = 23
	DropLabel.Parent = DropContainer



	local DropListGui = Instance.new("ScreenGui")
	DropListGui.Name = "TargetDropdownOverlay"
	DropListGui.ResetOnSpawn = false
	DropListGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	DropListGui.DisplayOrder = 999
	DropListGui.Enabled = false
	DropListGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

	local DropListFrame = Instance.new("Frame")
	DropListFrame.Name = "DropList"
	DropListFrame.Size = UDim2.new(0, 210, 0, 0)
	DropListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
	DropListFrame.BackgroundTransparency = 0.1
	DropListFrame.BorderSizePixel = 0
	DropListFrame.Visible = false
	DropListFrame.ClipsDescendants = true
	DropListFrame.ZIndex = 100
	DropListFrame.Parent = DropListGui

	local DLCorner = Instance.new("UICorner")
	DLCorner.CornerRadius = UDim.new(0, 16)
	DLCorner.Parent = DropListFrame

	local DLStroke = Instance.new("UIStroke")
	DLStroke.Color = Color3.fromRGB(255, 105, 180)
	DLStroke.Thickness = 1.5
	DLStroke.Transparency = 0.3
	DLStroke.Parent = DropListFrame



	local DLHeader = Instance.new("Frame")
	DLHeader.Size = UDim2.new(1, 0, 0, 46)
	DLHeader.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	DLHeader.BackgroundTransparency = 0.85
	DLHeader.BorderSizePixel = 0
	DLHeader.ZIndex = 102
	DLHeader.Parent = DropListFrame

	local DLHeaderCorner = Instance.new("UICorner")
	DLHeaderCorner.CornerRadius = UDim.new(0, 16)
	DLHeaderCorner.Parent = DLHeader



	local DLHeaderDivider = Instance.new("Frame")
	DLHeaderDivider.Size = UDim2.new(1, -20, 0, 1)
	DLHeaderDivider.Position = UDim2.new(0, 10, 1, -1)
	DLHeaderDivider.BackgroundColor3 = Color3.fromRGB(255, 182, 193)
	DLHeaderDivider.BackgroundTransparency = 0.5
	DLHeaderDivider.BorderSizePixel = 0
	DLHeaderDivider.ZIndex = 103
	DLHeaderDivider.Parent = DLHeader

	local DLHeaderIcon = Instance.new("ImageLabel")
	DLHeaderIcon.Size = UDim2.new(0, 20, 0, 20)
	DLHeaderIcon.Position = UDim2.new(0, 12, 0.5, -10)
	DLHeaderIcon.BackgroundTransparency = 1
	DLHeaderIcon.Image = "rbxassetid://101257615952289"
	DLHeaderIcon.ImageColor3 = Color3.fromRGB(255, 235, 245)
	DLHeaderIcon.ZIndex = 104
	DLHeaderIcon.Parent = DLHeader

	local DLHeaderLabel = Instance.new("TextLabel")
	DLHeaderLabel.Size = UDim2.new(1, -100, 1, 0)
	DLHeaderLabel.Position = UDim2.new(0, 38, 0, 0)
	DLHeaderLabel.BackgroundTransparency = 1
	DLHeaderLabel.Text = "Select Targets"
	DLHeaderLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
	DLHeaderLabel.TextSize = 12
	DLHeaderLabel.Font = Enum.Font.GothamBold
	DLHeaderLabel.TextXAlignment = Enum.TextXAlignment.Left
	DLHeaderLabel.ZIndex = 104
	DLHeaderLabel.Parent = DLHeader

	local DLClearBtn = Instance.new("ImageButton")
	DLClearBtn.Size = UDim2.new(0, 30, 0, 30)
	DLClearBtn.Position = UDim2.new(1, -72, 0.5, -15)
	DLClearBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
	DLClearBtn.BackgroundTransparency = 0.3
	DLClearBtn.BorderSizePixel = 0
	DLClearBtn.Image = "rbxassetid://114186457571628"
	DLClearBtn.ImageColor3 = Color3.fromRGB(255, 105, 180)
	DLClearBtn.ImageTransparency = 0
	DLClearBtn.ZIndex = 105
	DLClearBtn.AutoButtonColor = false
	DLClearBtn.Parent = DLHeader

	local DLClearCorner = Instance.new("UICorner")
	DLClearCorner.CornerRadius = UDim.new(0, 8)
	DLClearCorner.Parent = DLClearBtn

	local DLClearStroke = Instance.new("UIStroke")
	DLClearStroke.Color = Color3.fromRGB(255, 105, 180)
	DLClearStroke.Thickness = 1
	DLClearStroke.Transparency = 0.4
	DLClearStroke.Parent = DLClearBtn

	local DLCloseBtn = Instance.new("ImageButton")
	DLCloseBtn.Size = UDim2.new(0, 30, 0, 30)
	DLCloseBtn.Position = UDim2.new(1, -36, 0.5, -15)
	DLCloseBtn.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
	DLCloseBtn.BackgroundTransparency = 0.6
	DLCloseBtn.BorderSizePixel = 0
	DLCloseBtn.Image = "rbxassetid://89664565330598"
	DLCloseBtn.ImageColor3 = Color3.fromRGB(255, 255, 255)
	DLCloseBtn.ImageTransparency = 0
	DLCloseBtn.ZIndex = 105
	DLCloseBtn.AutoButtonColor = false
	DLCloseBtn.Parent = DLHeader

	local DLCloseBtnCorner = Instance.new("UICorner")
	DLCloseBtnCorner.CornerRadius = UDim.new(0, 8)
	DLCloseBtnCorner.Parent = DLCloseBtn

	local DLCloseBtnStroke = Instance.new("UIStroke")
	DLCloseBtnStroke.Color = Color3.fromRGB(255, 105, 180)
	DLCloseBtnStroke.Thickness = 1
	DLCloseBtnStroke.Transparency = 0.2
	DLCloseBtnStroke.Parent = DLCloseBtn

	local DLScroll = Instance.new("ScrollingFrame")
	DLScroll.Size = UDim2.new(1, 0, 1, -50)
	DLScroll.Position = UDim2.new(0, 0, 0, 50)
	DLScroll.BackgroundTransparency = 1
	DLScroll.ScrollBarThickness = 3
	DLScroll.ScrollBarImageColor3 = Color3.fromRGB(255, 182, 193)
	DLScroll.ScrollingDirection = Enum.ScrollingDirection.Y
	DLScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
	DLScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
	DLScroll.BorderSizePixel = 0
	DLScroll.ZIndex = 101
	DLScroll.Parent = DropListFrame

	local DLScrollLayout = Instance.new("UIListLayout")
	DLScrollLayout.Padding = UDim.new(0, 4)
	DLScrollLayout.SortOrder = Enum.SortOrder.LayoutOrder
	DLScrollLayout.Parent = DLScroll

	local DLScrollPad = Instance.new("UIPadding")
	DLScrollPad.PaddingTop = UDim.new(0, 6)
	DLScrollPad.PaddingBottom = UDim.new(0, 6)
	DLScrollPad.PaddingLeft = UDim.new(0, 7)
	DLScrollPad.PaddingRight = UDim.new(0, 7)
	DLScrollPad.Parent = DLScroll

	local dropOpen = false
	local playerItems = {}

	local function UpdateDropLabel()
		local names = {}
		for name, _ in pairs(selectedTargets) do
			table.insert(names, name)
		end
		if #names == 0 then
			DropLabel.Text = "Select Target"
			DropLabel.TextColor3 = Color3.fromRGB(180, 130, 165)
		elseif #names == 1 then
			DropLabel.Text = "  "..names[1]
			DropLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
		else
			DropLabel.Text = "  "..(#names).." targets selected"
			DropLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
		end
	end

	local function CreatePlayerItem(player)
		if playerItems[player.Name] then return end

		local ItemFrame = Instance.new("Frame")
		ItemFrame.Size = UDim2.new(1, 0, 0, 34)
		ItemFrame.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
		ItemFrame.BackgroundTransparency = selectedTargets[player.Name] and 0.75 or 0.9
		ItemFrame.BorderSizePixel = 0
		ItemFrame.ZIndex = 102
		ItemFrame.Parent = DLScroll

		local ItemCorner = Instance.new("UICorner")
		ItemCorner.CornerRadius = UDim.new(0, 10)
		ItemCorner.Parent = ItemFrame

		local ItemStroke = Instance.new("UIStroke")
		ItemStroke.Color = Color3.fromRGB(255, 105, 180)
		ItemStroke.Thickness = 1
		ItemStroke.Transparency = selectedTargets[player.Name] and 0.2 or 0.6
		ItemStroke.Parent = ItemFrame



		local CheckBox = Instance.new("Frame")
		CheckBox.Size = UDim2.new(0, 18, 0, 18)
		CheckBox.Position = UDim2.new(0, 9, 0.5, -9)
		CheckBox.BackgroundColor3 = selectedTargets[player.Name] and Color3.fromRGB(255, 105, 180) or Color3.fromRGB(30, 30, 40)
		CheckBox.BackgroundTransparency = selectedTargets[player.Name] and 0.2 or 0.3
		CheckBox.BorderSizePixel = 0
		CheckBox.ZIndex = 104
		CheckBox.Parent = ItemFrame

		local CheckCorner = Instance.new("UICorner")
		CheckCorner.CornerRadius = UDim.new(0, 5)
		CheckCorner.Parent = CheckBox

		local CheckStroke = Instance.new("UIStroke")
		CheckStroke.Color = Color3.fromRGB(255, 105, 180)
		CheckStroke.Thickness = 1
		CheckStroke.Transparency = 0.3
		CheckStroke.Parent = CheckBox

		local CheckMark = Instance.new("ImageLabel")
		CheckMark.Size = UDim2.new(0.75, 0, 0.75, 0)
		CheckMark.Position = UDim2.new(0.125, 0, 0.125, 0)
		CheckMark.BackgroundTransparency = 1
		CheckMark.Image = "rbxassetid://127907161081164"
		CheckMark.ImageColor3 = Color3.fromRGB(255, 105, 180)
		CheckMark.ImageTransparency = 0
		CheckMark.Visible = selectedTargets[player.Name] == true
		CheckMark.ZIndex = 105
		CheckMark.Parent = CheckBox

		local NameLabel = Instance.new("TextLabel")
		NameLabel.Size = UDim2.new(1, -40, 1, 0)
		NameLabel.Position = UDim2.new(0, 36, 0, 0)
		NameLabel.BackgroundTransparency = 1
		NameLabel.Text = player.Name
		NameLabel.TextColor3 = selectedTargets[player.Name] and Color3.fromRGB(255, 105, 180) or Color3.fromRGB(180, 130, 165)
		NameLabel.TextSize = 11
		NameLabel.Font = Enum.Font.GothamSemibold
		NameLabel.TextXAlignment = Enum.TextXAlignment.Left
		NameLabel.ZIndex = 104
		NameLabel.Parent = ItemFrame

		local ItemBtn = Instance.new("TextButton")
		ItemBtn.Size = UDim2.new(1, 0, 1, 0)
		ItemBtn.BackgroundTransparency = 1
		ItemBtn.Text = ""
		ItemBtn.ZIndex = 106
		ItemBtn.Parent = ItemFrame

		ItemBtn.MouseButton1Click:Connect(function()
			PlayClickSound()
			if selectedTargets[player.Name] then
				selectedTargets[player.Name] = nil
				CheckMark.Visible = false
				NameLabel.TextColor3 = Color3.fromRGB(180, 130, 165)
				TweenService:Create(ItemFrame, TweenInfo.new(0.15), {BackgroundTransparency = 0.9}):Play()
				TweenService:Create(ItemStroke, TweenInfo.new(0.15), {Transparency = 0.6}):Play()
				TweenService:Create(CheckBox, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(30, 30, 40), BackgroundTransparency = 0.3}):Play()
			else
				selectedTargets[player.Name] = true
				CheckMark.Visible = true
				NameLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
				TweenService:Create(ItemFrame, TweenInfo.new(0.15), {BackgroundTransparency = 0.75}):Play()
				TweenService:Create(ItemStroke, TweenInfo.new(0.15), {Transparency = 0.2}):Play()
				TweenService:Create(CheckBox, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 105, 180), BackgroundTransparency = 0.2}):Play()
			end
			UpdateDropLabel()
		end)

		ItemBtn.MouseEnter:Connect(function()
			TweenService:Create(ItemFrame, TweenInfo.new(0.12), {BackgroundTransparency = selectedTargets[player.Name] and 0.6 or 0.8}):Play()
		end)
		ItemBtn.MouseLeave:Connect(function()
			TweenService:Create(ItemFrame, TweenInfo.new(0.12), {BackgroundTransparency = selectedTargets[player.Name] and 0.75 or 0.9}):Play()
		end)

		playerItems[player.Name] = {Frame = ItemFrame, Check = CheckMark, Label = NameLabel}
	end

	local function RemovePlayerItem(playerName)
		if playerItems[playerName] then
			playerItems[playerName].Frame:Destroy()
			playerItems[playerName] = nil
			selectedTargets[playerName] = nil
			UpdateDropLabel()
		end
	end

	local function RefreshList()
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= LocalPlayer then
				CreatePlayerItem(p)
			end
		end
	end

	Players.PlayerAdded:Connect(function(p)
		if p ~= LocalPlayer then
			task.wait(0.2)
			CreatePlayerItem(p)
		end
	end)

	Players.PlayerRemoving:Connect(function(p)
		RemovePlayerItem(p.Name)
	end)

	local function GetDropListHeight()
		local playerCount = math.max(#Players:GetPlayers() - 1, 0)
		return math.min(playerCount * 35 + 56, 290)
	end

	local function PositionDropList()
		local absPos = DropContainer.AbsolutePosition
		local absSize = DropContainer.AbsoluteSize
		local screenH = workspace.CurrentCamera.ViewportSize.Y
		local screenW = workspace.CurrentCamera.ViewportSize.X
		local listH = GetDropListHeight()
		local listW = math.max(absSize.X, 200)

		local posY
		if absPos.Y + absSize.Y + 4 + listH > screenH then
			posY = absPos.Y - listH - 4
		else
			posY = absPos.Y + absSize.Y + 4
		end

		local posX = math.min(absPos.X, screenW - listW - 4)
		posX = math.max(posX, 4)
		posY = math.max(posY, 4)

		DropListFrame.Position = UDim2.new(0, posX, 0, posY)
		DropListFrame.Size = UDim2.new(0, listW, 0, listH)
	end

	RunService.RenderStepped:Connect(function()
		if dropOpen and DropListFrame.Visible then
			local absPos = DropContainer.AbsolutePosition
			local absSize = DropContainer.AbsoluteSize
			local screenH = workspace.CurrentCamera.ViewportSize.Y
			local screenW = workspace.CurrentCamera.ViewportSize.X
			local listH = DropListFrame.AbsoluteSize.Y
			local listW = DropListFrame.AbsoluteSize.X

			local posY
			if absPos.Y + absSize.Y + 4 + listH > screenH then
				posY = absPos.Y - listH - 4
			else
				posY = absPos.Y + absSize.Y + 4
			end

			local posX = math.min(absPos.X, screenW - listW - 4)
			posX = math.max(posX, 4)

			posY = math.max(posY, 4)

			DropListFrame.Position = UDim2.new(0, posX, 0, posY)
		end
	end)

	local function CloseDropdown()
		if not dropOpen then return end
		dropOpen = false
		TweenService:Create(DropListFrame, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Size = UDim2.new(0, math.max(DropContainer.AbsoluteSize.X, 210), 0, 0)
		}):Play()
		TweenService:Create(DropContainerStroke, TweenInfo.new(0.2), {Transparency = 0.4}):Play()
		task.delay(0.16, function()
			DropListFrame.Visible = false
			DropListGui.Enabled = false
		end)
	end

	UserInputService.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 and dropOpen then
			local mPos = UserInputService:GetMouseLocation()
			local fPos = DropListFrame.AbsolutePosition
			local fSize = DropListFrame.AbsoluteSize
			local bPos = DropContainer.AbsolutePosition
			local bSize = DropContainer.AbsoluteSize
			local inList = mPos.X >= fPos.X and mPos.X <= fPos.X + fSize.X and mPos.Y >= fPos.Y and mPos.Y <= fPos.Y + fSize.Y
			local inBtn = mPos.X >= bPos.X and mPos.X <= bPos.X + bSize.X and mPos.Y >= bPos.Y and mPos.Y <= bPos.Y + bSize.Y
			if inBtn then
				CloseDropdown()
				return
			end
			if not inList then
				CloseDropdown()
			end
		end
	end)

	DropButton.MouseButton1Click:Connect(function()
		PlayClickSound()
		if dropOpen then return end
		dropOpen = true
		RefreshList()
		PositionDropList()
		DropListGui.Enabled = true
		DropListFrame.Visible = true
		TweenService:Create(DropListFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = UDim2.new(0, math.max(DropContainer.AbsoluteSize.X, 210), 0, GetDropListHeight())
		}):Play()
		TweenService:Create(DropContainerStroke, TweenInfo.new(0.2), {Transparency = 0}):Play()
	end)

	DLCloseBtn.MouseEnter:Connect(function()
		TweenService:Create(DLCloseBtn, TweenInfo.new(0.12), {BackgroundTransparency = 0.3}):Play()
	end)
	DLCloseBtn.MouseLeave:Connect(function()
		TweenService:Create(DLCloseBtn, TweenInfo.new(0.12), {BackgroundTransparency = 0.6}):Play()
	end)

	DLClearBtn.MouseEnter:Connect(function()
		TweenService:Create(DLClearBtn, TweenInfo.new(0.12), {BackgroundTransparency = 0.1}):Play()
	end)
	DLClearBtn.MouseLeave:Connect(function()
		TweenService:Create(DLClearBtn, TweenInfo.new(0.12), {BackgroundTransparency = 0.3}):Play()
	end)

	DLCloseBtn.MouseButton1Click:Connect(function()
		PlayClickSound()
		CloseDropdown()
	end)

	DLClearBtn.MouseButton1Click:Connect(function()
		PlayClickSound()
		local toReset = {}
		for name, _ in pairs(selectedTargets) do
			table.insert(toReset, name)
		end
		for _, name in ipairs(toReset) do
			selectedTargets[name] = nil
			if playerItems[name] then
				playerItems[name].Check.Visible = false
				playerItems[name].Label.TextColor3 = Color3.fromRGB(180, 130, 165)
				TweenService:Create(playerItems[name].Frame, TweenInfo.new(0.15), {BackgroundTransparency = 0.9}):Play()
				TweenService:Create(playerItems[name].Frame:FindFirstChildOfClass("UIStroke"), TweenInfo.new(0.15), {Transparency = 0.6}):Play()
				local cb = playerItems[name].Check.Parent
				TweenService:Create(cb, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(30, 30, 40), BackgroundTransparency = 0.3}):Play()
			end
		end
		UpdateDropLabel()
	end)

	RefreshList()
	return DropContainer
end

CreateTargetDropdown(MurdererContent)

local KillTargetBtn = CreateShimmerButton(MurdererContent, "Kill Target")

KillTargetBtn.MouseButton1Click:Connect(function()
	local knife = LP.Backpack:FindFirstChild("Knife")
	if knife then
		LP.Character.Humanoid:EquipTool(knife)
	end
	task.wait(0.1)
	local heldKnife = LP.Character:FindFirstChild("Knife")
	if heldKnife then
		local event = heldKnife:WaitForChild("Events", 2):WaitForChild("HandleTouched", 2)
		if event then
			for _, player in pairs(Players:GetPlayers()) do
				if selectedTargets[player.Name] and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
					event:FireServer(player.Character.HumanoidRootPart)
				end
			end
		end
	end
end)

local TeleportTargetBtn = CreateShimmerButton(MurdererContent, "Teleport to Target")

TeleportTargetBtn.MouseButton1Click:Connect(function()
	local targetName = next(selectedTargets)
	if not targetName then return end
	local target = Players:FindFirstChild(targetName)
	if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
		LP.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
	end
end)

local KillAuraControl, GetKillAuraEnabled, GetKillAuraValue, SetKillAura, SetKillAuraValue = CreateToggleWithSlider(MurdererContent, "Kill Aura", 5)

RunService.RenderStepped:Connect(function()
	if not GetKillAuraEnabled() then return end
	local character = LocalPlayer.Character
	if not character then return end
	local heldKnife = character:FindFirstChild("Knife")
	if not heldKnife then return end
	local event = heldKnife:FindFirstChild("Events") and heldKnife.Events:FindFirstChild("HandleTouched")
	if not event then return end
	local hrp = character:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local rawValue = GetKillAuraValue() or 5
	local maxDistance = math.clamp(rawValue, 5, 20)
	for _, target in pairs(Players:GetPlayers()) do
		if target ~= LocalPlayer and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
			if next(selectedTargets) ~= nil and not selectedTargets[target.Name] then continue end
			local targetHrp = target.Character.HumanoidRootPart
			local targetHum = target.Character:FindFirstChild("Humanoid")
			if targetHum and targetHum.Health > 0 then
				local distance = (hrp.Position - targetHrp.Position).Magnitude
				if distance <= maxDistance then
					pcall(function()
						event:FireServer(targetHrp)
					end)
				end
			end
		end
	end
end)

local KillAllBtn = CreateShimmerButton(MurdererContent, "Kill All")

KillAllBtn.MouseButton1Click:Connect(function()
	local knife = LP.Backpack:FindFirstChild("Knife")
	if knife then
		LP.Character.Humanoid:EquipTool(knife)
	end
	task.wait(0.1)
	local heldKnife = LP.Character:FindFirstChild("Knife")
	if heldKnife then
		local event = heldKnife:WaitForChild("Events", 2):WaitForChild("HandleTouched", 2)
		if event then
			for _, target in pairs(Players:GetPlayers()) do
				if target ~= LP and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
					event:FireServer(target.Character.HumanoidRootPart)
				end
			end
		end
	end
end)

local KillSheriffBtn = CreateShimmerButton(MurdererContent, "Kill Sheriff/Hero")

KillSheriffBtn.MouseButton1Click:Connect(function()
	local knife = LP.Backpack:FindFirstChild("Knife")
	if knife then
		LP.Character.Humanoid:EquipTool(knife)
	end
	task.wait(0.1)
	local heldKnife = LP.Character:FindFirstChild("Knife")
	if heldKnife then
		local event = heldKnife:WaitForChild("Events", 2):WaitForChild("HandleTouched", 2)
		if event then
			UpdateRoles()
			for _, target in pairs(Players:GetPlayers()) do
				if target ~= LP and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
					local isSheriff = (target.Name == Sheriff)
					local isHero = (target.Name == Hero or (roles[target.Name] and roles[target.Name].HasGun))
					if isSheriff or isHero then
						event:FireServer(target.Character.HumanoidRootPart)
					end
				end
			end
		end
	end
end)

local TeleportMurdererBtn = CreateShimmerButton(SheriffContent, "Teleport to Murderer")

TeleportMurdererBtn.MouseButton1Click:Connect(function()
	local murderer = nil
	for _, v in ipairs(game:GetService("Players"):GetPlayers()) do
		if v.Backpack:FindFirstChild("Knife") or (v.Character and v.Character:FindFirstChild("Knife")) then
			murderer = v
			break
		end
	end
	if murderer and murderer.Character and murderer.Character:FindFirstChild("HumanoidRootPart") then
		local targetRoot = murderer.Character.HumanoidRootPart
		local localCharacter = game.Players.LocalPlayer.Character
		if localCharacter and localCharacter:FindFirstChild("HumanoidRootPart") then
			localCharacter.HumanoidRootPart.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 6)
		end
	end
end)

local CONFIG_FOLDER = "SatomiV2"
local CONFIG_FILE = CONFIG_FOLDER .. "/config.json"

pcall(function()
	if not isfolder(CONFIG_FOLDER) then
		makefolder(CONFIG_FOLDER)
	end
end)

local function SaveConfigToFile()
	local ok, config = pcall(function()
		local c = {}
		c.SaveEnabled = true
		c.Toggles = {
			GunESP = GetGunESP(),
			MurdererESP = GetMurdererESP(),
			SheriffESP = GetSheriffESP(),
			InnocentESP = GetInnocentESP(),
			HeroESP = GetHeroESP(),
			AutoGetGun = GetAutoGetGun(),
			Walkspeed = GetWalkspeedEnabled(),
			WalkspeedValue = GetWalkspeedValue(),
			JumpBoost = GetJumpBoostEnabled(),
			JumpBoostValue = GetJumpBoostValue(),
			KillAura = GetKillAuraEnabled(),
			KillAuraValue = GetKillAuraValue(),
			AntiFling = AntiFlingEnabled,
			AnchorAll = GetAnchor(),
			throwEnabled = throwEnabled,
			IsInvisible = IsInvisible,
		}
		c.FloatingPositions = {}
		for _, fb in ipairs(FloatingButtons) do
			c.FloatingPositions[fb.Gui.Name] = {
				X = fb.Frame.Position.X.Scale,
				XO = fb.Frame.Position.X.Offset,
				Y = fb.Frame.Position.Y.Scale,
				YO = fb.Frame.Position.Y.Offset,
			}
		end
		return c
	end)
	if ok and config then
		pcall(function()
			writefile(CONFIG_FILE, HttpService:JSONEncode(config))
		end)
	end
end

local function DeleteConfigFile()
	pcall(function()
		if isfile(CONFIG_FILE) then
			delfile(CONFIG_FILE)
		end
	end)
end

local MiscSection, MiscContent = CreateSection(SettingsTab, "Misc", UDim2.new(1, 0, 0, 68))

local SaveConfigEnabled = false

local SaveConfigToggle, GetSaveConfig, SetSaveConfig = CreateModernToggle(MiscContent, "Save Config", function(enabled)
	SaveConfigEnabled = enabled
	if enabled then
		SaveConfigToFile()
	else
		DeleteConfigFile()
		SetGunESP(true)
		SetMurdererESP(true)
		SetSheriffESP(true)
		SetInnocentESP(true)
		SetHeroESP(true)
		SetWalkspeed(true)
		SetJumpBoost(false)
	end
end)

task.spawn(function()
	while true do
		task.wait(3)
		if SaveConfigEnabled then
			SaveConfigToFile()
		end
	end
end)



local GameSection, GameContent = CreateSection(SettingsTab, "Game", UDim2.new(1, 0, 0, 130))

local RejoinBtn = CreateShimmerButton(GameContent, "Rejoin")
local ServerHopBtn = CreateShimmerButton(GameContent, "ServerHop")

RejoinBtn.MouseButton1Click:Connect(function()
	if SaveConfigEnabled then SaveConfigToFile() end
	TeleportService:Teleport(game.PlaceId, LocalPlayer)
end)

ServerHopBtn.MouseButton1Click:Connect(function()
	if SaveConfigEnabled then SaveConfigToFile() end
	local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100"))
	for _, server in ipairs(servers.data) do
		if server.playing < server.maxPlayers and server.id ~= game.JobId then
			TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer)
			break
		end
	end
end)

SetWalkspeed(true)
WalkConnection = RunService.RenderStepped:Connect(function()
	if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
		LocalPlayer.Character.Humanoid.WalkSpeed = GetWalkspeedValue()
	end
end)

local dragging = false
local dragStart, startPos

TopBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
	end
end)

TopBar.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

local IndicatorHandleGui = Instance.new("ScreenGui")
IndicatorHandleGui.Name = "SatomiMM2Indicator"
IndicatorHandleGui.ResetOnSpawn = false
IndicatorHandleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
IndicatorHandleGui.IgnoreGuiInset = true
IndicatorHandleGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local IndicatorHandle = Instance.new("TextButton")
IndicatorHandle.Name = "IndicatorHandle"
IndicatorHandle.Size = UDim2.new(0, 160, 0, 36)
IndicatorHandle.Position = UDim2.new(0.5, 0, 0, 10)
IndicatorHandle.AnchorPoint = Vector2.new(0.5, 0)
IndicatorHandle.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
IndicatorHandle.BackgroundTransparency = 0.1
IndicatorHandle.BorderSizePixel = 0
IndicatorHandle.Text = ""
IndicatorHandle.AutoButtonColor = false
IndicatorHandle.Visible = false
IndicatorHandle.ClipsDescendants = true
IndicatorHandle.ZIndex = 10
IndicatorHandle.Parent = IndicatorHandleGui

local IHCorner = Instance.new("UICorner")
IHCorner.CornerRadius = UDim.new(0, 18)
IHCorner.Parent = IndicatorHandle

local IHStroke = Instance.new("UIStroke")
IHStroke.Color = Color3.fromRGB(255, 105, 180)
IHStroke.Thickness = 1.5
IHStroke.Transparency = 0.2
IHStroke.Parent = IndicatorHandle

local IHGradient = Instance.new("UIGradient")
IHGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(35, 20, 40)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 15, 30))
})
IHGradient.Rotation = 135
IHGradient.Parent = IndicatorHandle

local IHSheen = Instance.new("Frame")
IHSheen.Size = UDim2.new(1, 0, 0.45, 0)
IHSheen.Position = UDim2.new(0, 0, 0, 0)
IHSheen.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
IHSheen.BackgroundTransparency = 0.97
IHSheen.BorderSizePixel = 0
IHSheen.ZIndex = 11
IHSheen.Parent = IndicatorHandle

local IHSheenCorner = Instance.new("UICorner")
IHSheenCorner.CornerRadius = UDim.new(0, 18)
IHSheenCorner.Parent = IHSheen

local IHDot = Instance.new("Frame")
IHDot.Size = UDim2.new(0, 7, 0, 7)
IHDot.Position = UDim2.new(0, 14, 0.5, -3)
IHDot.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
IHDot.BorderSizePixel = 0
IHDot.ZIndex = 12
IHDot.Parent = IndicatorHandle

local IHDotCorner = Instance.new("UICorner")
IHDotCorner.CornerRadius = UDim.new(1, 0)
IHDotCorner.Parent = IHDot

local IHLabel = Instance.new("TextLabel")
IHLabel.Size = UDim2.new(1, 0, 1, 0)
IHLabel.Position = UDim2.new(0, 0, 0, 0)
IHLabel.BackgroundTransparency = 1
IHLabel.Text = "Satomi [ MM2 ]"
IHLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
IHLabel.TextSize = 13
IHLabel.Font = Enum.Font.GothamBold
IHLabel.TextXAlignment = Enum.TextXAlignment.Center
IHLabel.ZIndex = 12
IHLabel.Parent = IndicatorHandle

IndicatorHandle.MouseEnter:Connect(function()
	TweenService:Create(IndicatorHandle, TweenInfo.new(0.15), {BackgroundTransparency = 0}):Play()
	TweenService:Create(IHStroke, TweenInfo.new(0.15), {Transparency = 0}):Play()
end)

IndicatorHandle.MouseLeave:Connect(function()
	TweenService:Create(IndicatorHandle, TweenInfo.new(0.15), {BackgroundTransparency = 0.1}):Play()
	TweenService:Create(IHStroke, TweenInfo.new(0.15), {Transparency = 0.2}):Play()
end)

IndicatorHandle.MouseButton1Click:Connect(function()
	PlayClickSound()
	IndicatorHandle.Visible = false
	MainFrame.Visible = true
	TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 460, 0, 345)}):Play()
end)

CloseButton.MouseButton1Click:Connect(function()
	PlayClickSound()
	TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 460, 0, 0)}):Play()
	wait(0.22)
	MainFrame.Visible = false
	IndicatorHandle.Visible = true
end)

SwitchTab(HomeTab)

local function CreateFloatingButton(config)
	local BtnGui = Instance.new("ScreenGui")
	BtnGui.Name = config.Name.."Floating"
	BtnGui.ResetOnSpawn = false
	BtnGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	BtnGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

	local BlurFrame = Instance.new("Frame")
	BlurFrame.Name = "BlurBackground"
	BlurFrame.Size = config.Size
	BlurFrame.Position = config.Position
	BlurFrame.BackgroundColor3 = Color3.fromRGB(255, 182, 193)
	BlurFrame.BackgroundTransparency = 0.15
	BlurFrame.BorderSizePixel = 0
	BlurFrame.Parent = BtnGui

	local CornerRadius = Instance.new("UICorner")
	CornerRadius.CornerRadius = config.CornerRadius
	CornerRadius.Parent = BlurFrame

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 105, 180)
	Stroke.Transparency = 0.2
	Stroke.Thickness = 2
	Stroke.Parent = BlurFrame

	local Gradient = Instance.new("UIGradient")
	Gradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 192, 203)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 105, 180)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 20, 147))
	})
	Gradient.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.6),
		NumberSequenceKeypoint.new(0.5, 0.5),
		NumberSequenceKeypoint.new(1, 0.7)
	})
	Gradient.Rotation = 45
	Gradient.Parent = BlurFrame

	local Btn = Instance.new("TextButton")
	Btn.Name = config.Name or "Button"
	Btn.Size = UDim2.new(1, 0, 1, 0)
	Btn.BackgroundTransparency = 1
	Btn.Text = config.Text
	Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	Btn.TextSize = config.TextSize
	Btn.Font = Enum.Font.GothamBold
	Btn.TextStrokeTransparency = 0.7
	Btn.TextStrokeColor3 = Color3.fromRGB(199, 21, 133)
	Btn.Parent = BlurFrame

	local ClickSound = Instance.new("Sound")
	ClickSound.Name = "ClickSound"
	ClickSound.SoundId = "rbxassetid://6895079853"
	ClickSound.Volume = 1
	ClickSound.Parent = BtnGui

	local DragHandle = Instance.new("Frame")
	DragHandle.Name = "DragHandle"
	DragHandle.Size = config.DragHandleSize
	DragHandle.Position = config.DragHandlePos
	DragHandle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	DragHandle.BackgroundTransparency = 0.3
	DragHandle.BorderSizePixel = 0
	DragHandle.Parent = BlurFrame

	local HandleCorner = Instance.new("UICorner")
	HandleCorner.CornerRadius = UDim.new(1, 0)
	HandleCorner.Parent = DragHandle

	local IsDragging = false
	local DragStart, StartPos

	local fbEntry = nil

	DragHandle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			if fbEntry and fbEntry.Anchored then return end
			IsDragging = true
			DragStart = input.Position
			StartPos = BlurFrame.Position
			TweenService:Create(DragHandle, TweenInfo.new(0.2), {BackgroundTransparency = 0.1}):Play()
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if IsDragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
			local delta = input.Position - DragStart
			BlurFrame.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + delta.X, StartPos.Y.Scale, StartPos.Y.Offset + delta.Y)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			IsDragging = false
			TweenService:Create(DragHandle, TweenInfo.new(0.2), {BackgroundTransparency = 0.3}):Play()
		end
	end)

	Btn.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			if config.OnPress then config.OnPress() end
			ClickSound:Play()
			TweenService:Create(BlurFrame, TweenInfo.new(0.05), {Size = config.PressSize}):Play()
			TweenService:Create(Gradient, TweenInfo.new(0.05), {
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 20, 147)),
					ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 105, 180)),
					ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 182, 193))
				})
			}):Play()
		end
	end)

	Btn.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			if config.OnRelease then config.OnRelease() end
			TweenService:Create(BlurFrame, TweenInfo.new(0.1, Enum.EasingStyle.Back), {Size = config.Size}):Play()
			TweenService:Create(Gradient, TweenInfo.new(0.1), {
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 192, 203)),
					ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 105, 180)),
					ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 20, 147))
				})
			}):Play()
		end
	end)

	TweenService:Create(Stroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {Transparency = 0.05}):Play()

	local entry = {
		Gui = BtnGui,
		Frame = BlurFrame,
		Button = Btn,
		Gradient = Gradient,
		Stroke = Stroke,
		DragHandle = DragHandle,
		Anchored = false,
		SetVisible = function(visible)
			BtnGui.Enabled = visible
		end
	}
	fbEntry = entry
	table.insert(FloatingButtons, entry)

	return BtnGui, BlurFrame, Btn
end

local roleCache = {}

task.spawn(function()
	while true do
		local success, data = pcall(function()
			local remote = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
			if remote then return remote:InvokeServer() end
		end)
		if success and type(data) == "table" then roleCache = data end
		task.wait(1.5)
	end
end)

local function getMurderer()
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= LP then
			local char = p.Character
			local backpack = p:FindFirstChild("Backpack")
			if (char and char:FindFirstChild("Knife")) or (backpack and backpack:FindFirstChild("Knife")) then
				return p
			end
		end
	end
	for name, data in pairs(roleCache) do
		if data.Role == "Murderer" then
			local p = Players:FindFirstChild(name)
			if p then return p end
		end
	end
	return nil
end

local function fireGun()
	local target = getMurderer()
	if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
		local targetHRP = target.Character.HumanoidRootPart
		local gun = LP.Character:FindFirstChild("Gun") or (LP.Backpack and LP.Backpack:FindFirstChild("Gun"))
		if gun and gun:FindFirstChild("Shoot") then
			local velocity = targetHRP.Velocity
			local predictedPos = targetHRP.Position + (velocity * 0.12)
			local cf = CFrame.new(targetHRP.Position, predictedPos)
			gun.Shoot:FireServer(cf, CFrame.new(predictedPos))
		end
	end
end

CreateFloatingButton({
	Name = "ShootMurderer",
	Size = UDim2.new(0, 200, 0, 80),
	Position = UDim2.new(0.85, -100, 0.85, -40),
	CornerRadius = UDim.new(0, 24),
	Text = "Shoot Murderer",
	TextSize = 20,
	DragHandleSize = UDim2.new(0, 40, 0, 6),
	DragHandlePos = UDim2.new(0.5, -20, 0, 10),
	PressSize = UDim2.new(0, 190, 0, 75),
	OnPress = function() end,
	OnRelease = function()
		fireGun()
	end
})

local throwEnabled = false

local whitelist = {
	["ketenimwryy"] = true,
	["ikoszx"] = true
}

local function isVisible(targetPart)
	local char = LP.Character
	if not char then return false end
	local origin = char.HumanoidRootPart.Position
	local direction = targetPart.Position - origin
	local rayParams = RaycastParams.new()
	rayParams.FilterType = Enum.RaycastFilterType.Exclude
	rayParams.FilterDescendantsInstances = {char}
	local result = workspace:Raycast(origin, direction, rayParams)
	if result and result.Instance:IsDescendantOf(targetPart.Parent) then
		return true
	end
	return false
end

local function throwAction()
	if not throwEnabled then return end
	local knife = LP.Character:FindFirstChild("Knife")
	if not knife then return end
	local near, dist = nil, math.huge
	for _, v in pairs(Players:GetPlayers()) do
		if v ~= LP and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
			if whitelist[v.Name] then continue end
			if next(selectedTargets) ~= nil and not selectedTargets[v.Name] then continue end
			local hrp = v.Character.HumanoidRootPart
			local d = (LP.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
			if d < dist and isVisible(hrp) then
				dist = d
				near = v
			end
		end
	end
	if near and near.Character:FindFirstChild("HumanoidRootPart") then
		local targetHRP = near.Character.HumanoidRootPart
		local origin = LP.Character.HumanoidRootPart.Position
		local targetPos = targetHRP.Position
		local args = {
			[1] = CFrame.new(origin, targetPos) * CFrame.Angles(1.4531978368759155, 0.04432811588048935, 1.6501713991165161),
			[2] = CFrame.new(targetPos) * CFrame.Angles(-0, 0, -0)
		}
		if knife:FindFirstChild("Events") and knife.Events:FindFirstChild("KnifeThrown") then
			knife.Events.KnifeThrown:FireServer(unpack(args))
		end
	end
end

CreateFloatingButton({
	Name = "ThrowKnife",
	Size = UDim2.new(0, 160, 0, 60),
	Position = UDim2.new(0.85, -80, 0.75, -30),
	CornerRadius = UDim.new(0, 18),
	Text = "Throw: OFF",
	TextSize = 16,
	DragHandleSize = UDim2.new(0, 30, 0, 4),
	DragHandlePos = UDim2.new(0.5, -15, 0, 8),
	PressSize = UDim2.new(0, 150, 0, 55),
	OnPress = function() end,
	OnRelease = function()
		throwEnabled = not throwEnabled
		for _, fb in ipairs(FloatingButtons) do
			if fb.Gui.Name == "ThrowKnifeFloating" then
				fb.Button.Text = throwEnabled and "Throw: ON" or "Throw: OFF"
			end
		end
	end
})

RunService.RenderStepped:Connect(function()
	if throwEnabled then
		throwAction()
	end
end)

CreateFloatingButton({
	Name = "GetGun",
	Size = UDim2.new(0, 80, 0, 80),
	Position = UDim2.new(0.85, -40, 0.65, -40),
	CornerRadius = UDim.new(1, 0),
	Text = "Gun",
	TextSize = 20,
	DragHandleSize = UDim2.new(0, 40, 0, 6),
	DragHandlePos = UDim2.new(0.5, -20, 0, 8),
	PressSize = UDim2.new(0, 75, 0, 75),
	OnPress = function() end,
	OnRelease = function()
		local drop = workspace:FindFirstChild("GunDrop", true)
		local character = LP.Character
		local hrp = character and character:FindFirstChild("HumanoidRootPart")
		if drop and hrp then
			firetouchinterest(hrp, drop, 0)
			firetouchinterest(hrp, drop, 1)
		end
	end
})

if _G.a then
	for _, connection in pairs(_G.a) do
		connection:Disconnect()
	end
	_G.a = nil
end

repeat task.wait() until game.Players.LocalPlayer
local InvisPlayer = game.Players.LocalPlayer

local InvisCharacter = nil
local InvisHumanoid = nil
local InvisHumanoidRootPart = nil
local IsInvisible = false
local VisibleParts = {}

local function SetupInvisCharacter()
	InvisCharacter = InvisPlayer.Character or InvisPlayer.CharacterAdded:Wait()
	InvisHumanoid = InvisCharacter:WaitForChild("Humanoid")
	InvisHumanoidRootPart = InvisCharacter:WaitForChild("HumanoidRootPart")
	VisibleParts = {}
	for _, descendant in pairs(InvisCharacter:GetDescendants()) do
		if descendant:IsA("BasePart") and descendant.Transparency == 0 then
			table.insert(VisibleParts, descendant)
		end
	end
end

SetupInvisCharacter()

local InvisConnections = {}

InvisConnections[1] = RunService.Heartbeat:Connect(function()
	if IsInvisible then
		local OriginalCFrame = InvisHumanoidRootPart.CFrame
		local OriginalCameraOffset = InvisHumanoid.CameraOffset
		local DownCFrame = OriginalCFrame * CFrame.new(0, -200000, 0)
		InvisHumanoidRootPart.CFrame = DownCFrame
		InvisHumanoid.CameraOffset = DownCFrame:ToObjectSpace(CFrame.new(OriginalCFrame.Position)).Position
		RunService.RenderStepped:Wait()
		InvisHumanoidRootPart.CFrame = OriginalCFrame
		InvisHumanoid.CameraOffset = OriginalCameraOffset
	end
end)

InvisPlayer.CharacterAdded:Connect(function()
	IsInvisible = false
	for _, fb in ipairs(FloatingButtons) do
		if fb.Gui.Name == "InvisibleFloating" then
			fb.Button.Text = "Invisible"
		end
	end
	SetupInvisCharacter()
end)

CreateFloatingButton({
	Name = "Invisible",
	Size = UDim2.new(0, 100, 0, 34),
	Position = UDim2.new(0.5, -50, 0.08, 0),
	CornerRadius = UDim.new(0, 10),
	Text = "Invisible",
	TextSize = 12,
	DragHandleSize = UDim2.new(0, 20, 0, 3),
	DragHandlePos = UDim2.new(0.5, -10, 0, 4),
	PressSize = UDim2.new(0, 92, 0, 30),
	OnPress = function() end,
	OnRelease = function()
		IsInvisible = not IsInvisible
		if IsInvisible then
			for _, fb in ipairs(FloatingButtons) do
				if fb.Gui.Name == "InvisibleFloating" then
					fb.Button.Text = "Visible"
				end
			end
			for _, part in pairs(VisibleParts) do
				part.Transparency = 0.5
			end
		else
			for _, fb in ipairs(FloatingButtons) do
				if fb.Gui.Name == "InvisibleFloating" then
					fb.Button.Text = "Invisible"
				end
			end
			for _, part in pairs(VisibleParts) do
				part.Transparency = 0
			end
		end
	end
})

_G.a = InvisConnections

local savedCfg = nil
pcall(function()
	if isfile(CONFIG_FILE) then
		savedCfg = HttpService:JSONDecode(readfile(CONFIG_FILE))
	end
end)

if savedCfg and savedCfg.SaveEnabled then
	local cfg = savedCfg
	if cfg.Toggles then
		SetGunESP(cfg.Toggles.GunESP)
		SetMurdererESP(cfg.Toggles.MurdererESP)
		SetSheriffESP(cfg.Toggles.SheriffESP)
		SetInnocentESP(cfg.Toggles.InnocentESP)
		SetHeroESP(cfg.Toggles.HeroESP)
		SetAutoGetGun(cfg.Toggles.AutoGetGun)
		SetWalkspeed(cfg.Toggles.Walkspeed, cfg.Toggles.WalkspeedValue)
		SetJumpBoost(cfg.Toggles.JumpBoost, cfg.Toggles.JumpBoostValue)
		SetKillAura(cfg.Toggles.KillAura, cfg.Toggles.KillAuraValue)
		if cfg.Toggles.AntiFling then
			AntiFlingEnabled = true
			pcall(function()
				loadstring(game:HttpGet("https://raw.githubusercontent.com/sytcal/antifling/main/9999"))()
			end)
		end
		throwEnabled = cfg.Toggles.throwEnabled or false
		IsInvisible = cfg.Toggles.IsInvisible or false
		if cfg.Toggles.AnchorAll then
			SetAnchor(true)
			for _, fb in ipairs(FloatingButtons) do
				AnchoredPositions[fb.Gui.Name] = fb.Frame.Position
				fb.Anchored = true
				fb.DragHandle.Visible = false
			end
		end
	end
	if cfg.FloatingPositions then
		for _, fb in ipairs(FloatingButtons) do
			local pos = cfg.FloatingPositions[fb.Gui.Name]
			if pos then
				fb.Frame.Position = UDim2.new(pos.X, pos.XO, pos.Y, pos.YO)
			end
		end
	end
	SetSaveConfig(true)
	SaveConfigEnabled = true
end
